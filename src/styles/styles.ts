import { css } from "@emotion/react";

export const globalStyles = css`
  html,
  body,
  #home {
    height: 100%;
  }
  body {
    margin: 0;
  }
`;

export const contentContainer = css`
  width: 80%;
  max-width: 900px;
  margin-left: auto;
  margin-right: auto;
`;
